package com.protocol.processor

internal object Constants {

    private const val ANNOTATION_PACKAGE = "com.protocol.annotation"

    const val ANNOTATION_IMPL = "$ANNOTATION_PACKAGE.ProtocolImpl"

    const val ANNOTATION_PROXY = "$ANNOTATION_PACKAGE.Protocol"

    const val PROTOCOL_PROVIDER_PACKAGE = "com.protocol.provider"
}